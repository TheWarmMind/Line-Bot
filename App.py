from flask import Flask, request
import os
from linebot import LineBotApi, WebhookHandler
from linebot.models import MessageEvent, TextMessage, TextSendMessage

app = Flask(__name__)

line_bot_api = LineBotApi(os.getenv("CHANNEL_ACCESS_TOKEN"))
handler = WebhookHandler(os.getenv("CHANNEL_SECRET"))

@app.route("/callback", methods=['POST'])
def callback():
    signature = request.headers['X-Line-Signature']
    body = request.get_data(as_text=True)
    handler.handle(body, signature)
    return 'OK'

@handler.add(MessageEvent, message=TextMessage)
def handle_message(event):
    line_bot_api.reply_message(
        event.reply_token,
        TextSendMessage(text="🤍สวัสดีค่ะ WarmMind ยินดีต้อนรับนะคะ ไม่ว่าจะรู้สึกแบบไหน มาค่อยๆคุยกันได้เลยค่ะ ที่นี่ไม่มีการตัดสินมีแต่ความเข้าใจค่ะ")
    )

if __name__ == "__main__":
    port = int(os.environ.get("PORT", 10000))
    app.run(host="0.0.0.0", port=port)

@handler.add(MessageEvent, message=TextMessage)
def handle_message(event):
    user_text = event.message.text.lower()

    if "เหนื่อย" in user_text:
        reply = (
            "เหนื่อยก็พักได้นะ 🤍\n"
            "ไม่ต้องเก่ง ไม่ต้องเข้มแข็งตลอดก็ได้\n"
            "วันนี้ถ้าไหวแค่นั่งเฉย ๆ หายใจลึก ๆ สักหน่อยก็พอแล้ว\n"
            "ถ้าอยากระบาย บอกเราได้เลย เราฟังอยู่นะ"
        )

    elif "ท้อ" in user_text or "เครียด" in user_text:
        reply = (
            "เป็นอะไรไหม 🫂\n"
            "ถ้าอยากระบาย พิมพ์มาได้เลยนะ เราอยู่ตรงนี้\n"
            "ไม่ต้องเรียบเรียง ไม่ต้องเกรงใจ\n"
            "หรือถ้ายังไม่อยากเล่า เดี๋ยวช่วยคิดวิธีผ่อนคลายให้ก็ได้ 🩵"
        )

    elif "เศร้า" in user_text:
        reply = (
            "เราอยู่ตรงนี้นะ 🤍\n"
            "ความเศร้ามันหนักจริง ๆ บางทีก็มาแบบไม่มีเหตุผล\n"
            "ไม่ต้องโทษตัวเองเลยที่รู้สึกแบบนี้\n"
            "ถ้าวันนี้มันไม่ไหว ก็ไม่เป็นไรนะ\n"
            "อยากเล่าไหม หรืออยากให้เรานั่งเป็นเพื่อนเงียบ ๆ ก็ได้ 🫂"
        )

    elif "เสียใจ" in user_text:
        reply = (
            "เรารับรู้นะว่าเธอเสียใจอยู่ 🤍\n"
            "บางทีคำนี้มันหนักมากจนไม่รู้จะอธิบายยังไง\n"
            "ไม่เป็นไรเลยที่วันนี้จะอ่อนแอ\n"
            "ถ้าอยากระบาย เราอยู่ตรงนี้นะ\n"
            "เธอไม่ต้องแบกมันคนเดียว 🌱"
        )

    elif "ไม่ไหว" in user_text:
        reply = (
            "ได้ยินคำนี้แล้วรู้เลยว่ามันหนักมากจริง ๆ 🫂\n"
            "เราอยู่ตรงนี้นะ คุณไม่ต้องรับมันคนเดียว\n\n"
            "ลองหายใจช้า ๆ ไปด้วยกันนะ\n"
            "หายใจเข้า 4 วิ – กลั้น 2 วิ – ผ่อนออก 6 วิ\n"
            "ทำสัก 3 รอบก็พอ\n\n"
            "ถ้ากลัวว่าจะทำร้ายตัวเอง โทร 1323 ได้ตลอด 24 ชม.\n"
            "หรือบอกเราตรงนี้ก็ได้ว่า ตอนนี้อันตรายไหม 🤍"
        )

    elif "ขอระบาย" in user_text:
        reply = (
            "ได้เลยนะ เราอยู่ตรงนี้ให้ 💛\n"
            "ระบายออกมาได้เต็มที่ ไม่ต้องเรียบเรียง\n"
            "จะเหนื่อย เศร้า โกรธ หรือสับสนก็พูดมาเถอะ\n"
            "พิมพ์แค่ว่า “มันเหนื่อยมาก” ก็ยังได้\n"
            "เราอยู่ตรงนี้นะ 🤍"
        )

    else:
        reply = "เราฟังอยู่นะ 🤍 พิมพ์มาได้เลย"

    line_bot_api.reply_message(
        event.reply_token,
        TextSendMessage(text=reply)
    )
